# data/panos/

Input images go here (not tracked in git -- see `.gitignore`). Any
street-level or equirectangular photo works; drop `.jpg`/`.png` files in this
folder and point the pipeline scripts at it, e.g.:

```bash
python ground_floor_sam_gdinov2.5.py data/panos
```
