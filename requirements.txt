# ---------------------------------------------------------------------------
# Python 3.11 recommended.
#
# 1) Install a torch build that matches your CUDA FIRST, e.g. for CUDA 12.8:
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#    (CPU-only works too but is slow: pip install torch torchvision)
#
# 2) Then: pip install -r requirements.txt
# ---------------------------------------------------------------------------

transformers>=5.12          # CLIP, SegFormer, and Grounding DINO's BERT text encoder
segment-anything            # SAM (loads sam_vit_h_4b8939.pth)
groundingdino-py            # Grounding DINO (loads groundingdino_swinb_cogcoor.pth + ships the SwinB config)
paddleocr>=3.5.0            # PP-OCRv6 sign text (el/en); engine="transformers" avoids needing paddlepaddle
opencv-python
pillow
numpy

# torch / torchvision are installed in step 1 above (CUDA-specific), not pinned here.

# ground_floor_sam_gdinov2.5.py only (--use-vlm): Qwen2.5-VL ground-floor
# unit-splitting + reclassification, 4-bit quantized by default.
accelerate                  # needed for device_map= when loading the VLM
bitsandbytes                # needed for 4-bit (NF4) VLM quantization
